apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp-node.fullname" . }}
  labels:
    {{- include "webapp-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp-node.selectorLabels" . | nindent 4 }}
